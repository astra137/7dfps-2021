name: CI

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    environment: ludservilo
    runs-on: ubuntu-latest
    steps:
      - name: Cloning repo
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Push to Dokku
        uses: dokku/github-action@v1.0.1
        with:
          git_remote_url: 'ssh://dokku@ludservilo.astra137.dev:26903/7dfps'
          ssh_private_key: ${{ secrets.SSH_PRIVATE_KEY }}
